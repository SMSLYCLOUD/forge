/// Input handler for keyboard and mouse events
pub struct InputHandler {
    // Placeholder
}

impl InputHandler {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for InputHandler {
    fn default() -> Self {
        Self::new()
    }
}
