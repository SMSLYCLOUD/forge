/// Forge window wrapper around winit
pub struct ForgeWindow {
    // Placeholder - will be expanded with winit integration
}

impl ForgeWindow {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ForgeWindow {
    fn default() -> Self {
        Self::new()
    }
}
